# TechLogistics Challenge 03 — dependencias (versiones EXACTAS probadas)
# Probado con Python 3.11.4. El pipeline completo (notebook + informe) se ejecuta
# de cero sin errores con estas versiones. Para una réplica byte-a-byte de todo el
# árbol de dependencias, usar en su lugar: pip install -r requirements-lock.txt
#
#   py -m venv venv && venv\Scripts\activate && pip install -r requirements.txt

# Core data stack
numpy
pandas
scipy

# Time series / econometrics
statsmodels==0.14.6
pmdarima==2.1.1

# Graphs / networks
networkx

# Visualization
plotly>=6.9.0
matplotlib>=3.11.1
seaborn
contextily       # basemap real (teselas OSM) bajo el scatter estático de T1
kaleido          # incluido por requerimiento del reto (export estático de Plotly).
                        # NOTA: en Windows su primer write_image puede bloquear el kernel,
                        # por eso las figuras PNG se exportan con matplotlib (equivalente).

# Machine learning utils
scikit-learn>=1.9.0

# Notebook / reporting
nbformat
nbconvert
jupyter
reportlab>=5.0.0        # generación programática del PDF
